var assert = require('assert-extras'),
    common = require('./common');

var afterCount = 0;

var tests = {
    after: function(done) {
        afterCount++;
        done();
    },
    'after count should still be 0': function(done) {
        assert.ok(afterCount === 0, 'after count should still be 0');
        setTimeout(done, fakeDelay);
    },
    'after count should be incremented to 1': function(done) {
        assert.ok(afterCount === 1, 'after count should be incremented to 1');
        setTimeout(done, fakeDelay);
    }
};

module.exports = tests;
