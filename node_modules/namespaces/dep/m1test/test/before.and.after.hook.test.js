var assert = require('assert-extras'),
    common = require('./common');

var count = 1;

var tests = {
    before: function(done) {
        count--;
        done();
    },
    after: function(done) {
        count++;
        done();
    },
    'count should be at 0': function(done) {
        assert.ok(count === 0, 'count should be at 0');
        setTimeout(done, fakeDelay);
    },
    'count should still be at 0': function(done) {
        assert.ok(count === 0, 'count should still be at 0');
        setTimeout(done, fakeDelay);
    }
};

module.exports = tests;
