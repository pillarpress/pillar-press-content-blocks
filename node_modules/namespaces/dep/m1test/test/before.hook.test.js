var assert = require('assert-extras'),
    common = require('./common');

var beforeCount = 0;

var tests = {
    before: function(done) {
        beforeCount++;
        done();
    },
    'before count should be incremented to 1': function(done) {
        assert.ok(beforeCount === 1, 'before count should be incremented to 1');
        setTimeout(done, fakeDelay);
    },
    'before count should be incremented to 2': function(done) {
        assert.ok(beforeCount === 2, 'before count should be incremented to 2');
        setTimeout(done, fakeDelay);
    }
};

module.exports = tests;
