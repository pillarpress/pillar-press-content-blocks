var assert = require('assert-extras'),
    common = require('../common');

var tests = {
    'failure test': function(done) {
        assert.ok(false, 'purposely failing to make sure we can fail gracefully');
        setTimeout(done, fakeDelay);
    }
};

module.exports = tests;
