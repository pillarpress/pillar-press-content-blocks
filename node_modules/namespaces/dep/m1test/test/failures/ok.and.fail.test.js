var assert = require('assert-extras'),
    common = require('../common');

var tests = {
    'one OK': function(done) {
        assert.ok(true);
        setTimeout(done, fakeDelay);
    },
    'mutliple OKs and a fail': function(done) {
        assert.ok(true);
        assert.ok(false, 'should see one passed test and one failed test');
        assert.ok(true);
        setTimeout(done, fakeDelay);
    }
};

module.exports = tests;
