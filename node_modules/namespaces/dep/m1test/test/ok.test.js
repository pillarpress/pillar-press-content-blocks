var assert = require('assert-extras'),
    common = require('./common');

var tests = {
    'one OK': function(done) {
        assert.ok(true);
        setTimeout(done, fakeDelay);
    },
    'mutliple OKs': function(done) {
        assert.ok(true);
        assert.ok(true);
        assert.ok(true);
        assert.ok(true);
        assert.ok(true);
        setTimeout(done, fakeDelay);
    }
};

module.exports = tests;
